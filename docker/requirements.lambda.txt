# Minimal requirements for Lambda inference
# Keep total package size under 512MB

# AWS SDK
boto3>=1.28.0

# Deep Learning (CPU only for Lambda)
torch==2.0.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
torchvision==0.15.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# Image Processing (minimal)
opencv-python-headless==4.8.0.76
numpy==1.24.3
Pillow==10.0.0

# Configuration
python-dotenv==1.0.0

